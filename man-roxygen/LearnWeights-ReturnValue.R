#' @return By default a \code{\link[ndl2]{ndlLearner}} object is returned,
#'   but if \code{asMatrix} is set to \code{TRUE}, the learned weights are
#'   returned as a matrix with cues as rows and outcomes as columns.
